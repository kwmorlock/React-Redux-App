import React, {useState} from 'react';

const Movie = (props) => {
return (
    <div>
       <p> {props.movie.title} </p>
       <p> {props.movie.title} </p>
       <p> {props.movie.title} </p>
    </div>
)
}